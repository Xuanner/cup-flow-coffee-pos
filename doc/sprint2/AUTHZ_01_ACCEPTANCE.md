# US-S2-AUTHZ-01 验收记录

| 项目 | 结果 |
| --- | --- |
| Story | `US-S2-AUTHZ-01` 未登录用户访问受保护页面 |
| 验收 Task | `TASK-S2-AUTHZ-01-02` |
| 验收日期 | 2026-08-28 |
| 环境 | Node.js 24、Vitest、Testing Library、Vite |
| 结论 | 通过；AUTHZ-02 顺序执行门已解锁并完成 |

## 验收标准与证据

| 验收标准 | 自动化或检查证据 | 结果 |
| --- | --- | --- |
| `/pos`、`/orders`、`/products`、`/dashboard` 及子路由要求登录 | `TC-S2-FE-ROUTE-001` 参数化覆盖四类入口和子地址 | 通过 |
| 未登录进入登录页且不渲染受保护内容 | 启动身份检查失败后仅显示登录页；业务标题和主导航不存在 | 通过 |
| 仅保存规范化站内相对返回地址 | `TC-S2-FE-ROUTE-002`、`003`；拒绝协议、双斜杠、反斜杠、编码变体和敏感参数 | 通过 |
| 有权目标恢复；无权、无效或不存在目标回默认页 | `TC-S2-FE-ROUTE-002`、`004`；返回目标再次按已知路由和当前角色校验 | 通过 |
| 已登录访问不存在路由显示 404 | `TC-S2-FE-ROUTE-009`；未知地址不误显示 403 | 通过 |
| 已登录访问 `/login` 进入角色默认页 | `TC-S2-FE-ROUTE-005` | 通过 |

## 执行记录

```text
npm run check
typecheck、lint、format:check 通过
Test Files: 12 passed；Tests: 76 passed
生产构建成功
```

认证守卫位于角色守卫之外，身份未知或未登录时不会先作角色判断，也不会短暂渲染受保护页面。返回目标只保存在路由内存状态中，不包含会话凭证。

## 后续交付门

- `TASK-S2-AUTHZ-01-01`、`02` 均为 `Done`。
- `US-S2-AUTHZ-02` 已按顺序完成。

# US-S2-AUTHZ-02 验收记录

| 项目 | 结果 |
| --- | --- |
| Story | `US-S2-AUTHZ-02` 按角色查看和访问页面 |
| 验收 Task | `TASK-S2-AUTHZ-02-02` |
| 验收日期 | 2026-08-28 |
| 环境 | Node.js 24、Vitest、Testing Library、Vite |
| 结论 | 通过；AUTHZ-03 顺序执行门已解锁并完成 |

## 验收标准与证据

| 验收标准 | 自动化或检查证据 | 结果 |
| --- | --- | --- |
| 收银员只看到 POS 和订单入口 | `TC-S2-FE-ROUTE-006` | 通过 |
| 管理员看到 POS、订单、商品与经营看板 | `TC-S2-FE-ROUTE-007`；前端权限模型明确 `ADMIN` 继承 `CASHIER` | 通过 |
| 收银员直达商品或看板显示 403，不渲染业务内容 | `TC-S2-FE-ROUTE-008` 参数化覆盖两个入口；页面提供返回 `/pos` 动作 | 通过 |
| 管理员可直接访问四个业务模块 | 管理员菜单与角色边界测试覆盖四入口 | 通过 |
| 刷新或权限恢复期间不显示无权菜单 | `TC-S2-FE-ROUTE-010`；认证确认前仅显示应用级加载 | 通过 |
| 权限未知时不使用陈旧客户端角色放行 | 用户身份只在当前会话恢复成功后写入内存；角色边界缺少用户时回登录页 | 通过 |
| 页面隐藏不作为后端授权证据 | 后端独立鉴权证据登记于 `AUTHZ_03_ACCEPTANCE.md` | 通过 |

## 执行记录

```text
npm run check
typecheck、lint、format:check 通过
Test Files: 12 passed；Tests: 76 passed
生产构建成功
```

系统内部页不作为业务导航入口；403 与 404 使用独立路由，避免将不存在页面错误解释为角色不足。

## 后续交付门

- `TASK-S2-AUTHZ-02-01`、`02` 均为 `Done`。
- `US-S2-AUTHZ-03` 已按顺序完成。
